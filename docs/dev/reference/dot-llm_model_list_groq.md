# Get Groq Models

Returns a list of available models in groq, excluding whisper and vision
models (for audio and images) See <https://console.groq.com/docs/models>
for more information.

## Usage

``` r
.llm_model_list_groq()
```

## Value

a table of model info
