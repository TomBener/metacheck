# Parse crossref query items into a table

Parse crossref query items into a table

## Usage

``` r
.crossref_query_parse(items, min_score, select)
```

## Arguments

- items:

  list of items from CrossRef query response

- min_score:

  minimum score threshold

- select:

  fields to select

## Value

a data frame
