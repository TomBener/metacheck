# Read bibr JSON file

Read bibr JSON file

## Usage

``` r
read_bibr(file_path, include_images = FALSE)
```

## Arguments

- file_path:

  path to the JSON file

- include_images:

  whether to include images in the figures table of the paper object
  (they make object size larger)

## Value

a paper object
