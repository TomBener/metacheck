# Parse an OSF API response into a data frame

Parse an OSF API response into a data frame

## Usage

``` r
.osf_parse_response(resp, pb = NULL)
```

## Arguments

- resp:

  an httr2 response

- pb:

  a progress bar

## Value

a single-row data frame
